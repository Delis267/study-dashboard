# Phase 1 - Testprogramme starten

Im Ordner `Phase1` ausführen:

```powershell
python run_tests.py
```

Das startet die Konsolentests für CSV-Repository und Prognose.

Der GUI-Machbarkeitstest kann optional separat gestartet werden:

```powershell
cd src
python gui_test.py
```
